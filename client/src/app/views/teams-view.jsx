import { Component } from 'react';

class TeamsView extends Component {
  constructor(...args) {
    super(...args);
  }

  render() {
    return (
      <div>TeamsView</div>
    );
  }
}

export default TeamsView;
